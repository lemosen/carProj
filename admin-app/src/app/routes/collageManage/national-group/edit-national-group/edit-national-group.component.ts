
import { Component, OnInit, ViewEncapsulation } from '@angular/core';
import { ActivatedRoute, ParamMap, Router } from '@angular/router';
import { NzMessageService, NzModalService } from 'ng-zorro-antd';
import { SUCCESS } from '../../../models/constants.model';
import { NationalGroupService } from '../../../services/national-group.service';
import { NationalGroup } from '../../../models/original/national-group.model';

@Component({
  selector: 'edit-national-group',
  templateUrl: './edit-national-group.component.html',
  styleUrls: ['./edit-national-group.component.scss'],
  encapsulation: ViewEncapsulation.None
})
export class EditNationalGroupComponent implements OnInit {

    submitting = false;

nationalGroup: NationalGroup;

    constructor(private route: ActivatedRoute,private router:Router,private nationalGroupService: NationalGroupService, public msgSrv:
        NzMessageService,private modalService: NzModalService) { }

    ngOnInit() {
        this.route.params.subscribe((params: ParamMap) => {
            this.getById(params["objId"]);
        });
    }

    getById(objId){
        this.nationalGroupService.getVoById(objId).subscribe(response => {
            if (response.result == SUCCESS) {
                this.nationalGroup = response.data;
            } else {
                this.msgSrv.error('请求失败', response.message);
            }
        }, error => {
            this.msgSrv.error('请求错误', error.message);
        });
    }

    onTransmitFormValueSubscription(event) {
        if (event) {
            this.confirmSub(event)
        }
    }

    confirmSub(formValue){
        if (formValue) {
            this.submitting = true;
            const messageId = this.msgSrv.loading("正在提交...").messageId;
            formValue.obj.id = this.nationalGroup.id;
            this.nationalGroupService.update(formValue.obj).subscribe(response => {
                if (response.result == SUCCESS) {
                    this.msgSrv.success("操作成功");
                    this.router.navigate(['/dashboard/national-group/list']);
                } else {
                    this.msgSrv.error('请求失败'+response.message);
                }
                this.msgSrv.remove(messageId);
                this.submitting = false;
            }, error => {
                this.msgSrv.error('请求错误'+error.message);
                this.msgSrv.remove(messageId);
                this.submitting = false;
            });
        }
    }
}
